import os
import sys

# Add the parent directory to sys.path to import common_conf
sys.path.insert(0, os.path.abspath('..'))

from common_conf import *

# -- Language configuration --------------------------------------------------
language = 'en'

# -- Options for HTML output -------------------------------------------------
html_search_language = 'en'
